import React, { Component } from "react";
import SideNav from "./side-nav";
import Logo from "./logo";

class Support extends Component {
  render() {
    return (
      <div id="support" className="page-wrapper">
        <SideNav />
        <Logo />
      </div>
    );
  }
}

export default Support;
